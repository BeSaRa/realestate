import { FormatNumbersPipe } from './format-numbers.pipe';

describe('FormatNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
