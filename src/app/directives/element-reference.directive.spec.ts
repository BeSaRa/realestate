import { ElementReferenceDirective } from './element-reference.directive';

describe('ElementReferenceDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementReferenceDirective();
    expect(directive).toBeTruthy();
  });
});
