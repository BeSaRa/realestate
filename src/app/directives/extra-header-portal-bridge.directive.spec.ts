import { ExtraHeaderPortalBridgeDirective } from './extra-header-portal-bridge.directive';

describe('ExtraHeaderPortalBridgeDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtraHeaderPortalBridgeDirective();
    expect(directive).toBeTruthy();
  });
});
