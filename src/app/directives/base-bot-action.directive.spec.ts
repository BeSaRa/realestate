import { BaseBotActionDirective } from './base-bot-action.directive';

describe('BaseBotActionDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseBotActionDirective();
    expect(directive).toBeTruthy();
  });
});
