import { OnlyCurrentLangLettersDirective } from './only-current-lang-letters.directive';

describe('OnlyCurrentLangLettersDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyCurrentLangLettersDirective();
    expect(directive).toBeTruthy();
  });
});
