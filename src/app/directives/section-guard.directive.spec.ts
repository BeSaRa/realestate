import { SectionGuardDirective } from './section-guard.directive';

describe('SectionGuardDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionGuardDirective();
    expect(directive).toBeTruthy();
  });
});
