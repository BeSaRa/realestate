import { BaseChatMessagesContainerDirective } from './base-chat-messages-container.directive';

describe('BaseChatMessagesContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseChatMessagesContainerDirective();
    expect(directive).toBeTruthy();
  });
});
