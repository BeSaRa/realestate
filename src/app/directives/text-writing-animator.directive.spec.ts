import { TextWritingAnimatorDirective } from './text-writing-animator.directive';

describe('TextWritingAnimatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextWritingAnimatorDirective();
    expect(directive).toBeTruthy();
  });
});
