import { RentDefaultValues } from './rent-default-values';

describe('RentDefaultValues', () => {
  it('should create an instance', () => {
    expect(new RentDefaultValues()).toBeTruthy();
  });
});
