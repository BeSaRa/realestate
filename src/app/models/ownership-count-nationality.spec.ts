import { OwnershipCountNationality } from './ownership-count-nationality';

describe('OwnershipCountNationality', () => {
  it('should create an instance', () => {
    expect(new OwnershipCountNationality()).toBeTruthy();
  });
});
