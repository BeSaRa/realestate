export enum Durations {
  YEARLY = 1,
  HALF_YEARLY,
  QUARTER_YEARLY,
  MONTHLY,
  DURATION,
}

export enum DurationEndpoints {
  YEARLY = 'yearly',
  HALFY = 'halfy',
  SELL_QUARTERLY = 'quarterly',
  RENT_QUARTERLY = 'quartley',
  MONTHLY = 'monthly',
}
