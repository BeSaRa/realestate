export enum Durations {
  YEARLY = 1,
  HALF_YEARLY,
  QUARTER_YEARLY,
  DURATION,
}

export enum DurationTypes {
  YEARLY = 'yearly',
  HALFY = 'halfy',
  QUARTERLY = 'quarterly',
  MONTHLY = 'monthly',
}
